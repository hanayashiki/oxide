# Local-dev linker paths for the inkwell -> llvm-sys chain on macOS arm64
# with Homebrew-installed llvm@18. Override per-host as needed.
[env]
LLVM_SYS_180_PREFIX = "/opt/homebrew/opt/llvm@18"

[target.aarch64-apple-darwin]
rustflags = ["-L", "/opt/homebrew/opt/zstd/lib"]
